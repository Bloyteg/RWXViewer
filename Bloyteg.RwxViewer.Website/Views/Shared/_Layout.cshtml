<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8" />
        <meta name="renderArea" content="width=device-width, initial-scale=1.0">
        <title>@ViewBag.Title</title>

        @Styles.Render("~/Content/css")
        @RenderSection("scripts", required: false)
    </head>
    <body>
        @RenderBody()
    </body>
</html>